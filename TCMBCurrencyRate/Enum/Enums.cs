using System;
using System.Collections.Generic;
using System.Text;

namespace TCMBCurrencyRate.Enum
{
    public enum Sorting
    {
        DESC = 0,
        ASC = 1
    }
    public enum Format
    {
        None = -1,
        XML = 0,
        JSON = 1,
        CSV = 2
    }
}
